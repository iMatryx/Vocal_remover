# Redis connection settings
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_DB=0

# Optional explicit Celery URLs; leave blank to use the Redis URL built above
CELERY_BROKER_URL=
CELERY_RESULT_BACKEND=

# Demucs model: htdemucs (fast, great quality) or htdemucs_ft (slower, best quality)
DEMUCS_MODEL=htdemucs

# Cores used for separation. Fewer = cooler, quieter machine but slower.
# Default is 60% of available cores.
DEMUCS_THREADS=6

# Overlap between processed chunks: higher = better quality, slower
DEMUCS_OVERLAP=0.25


# How long uploads and separated stems are kept before automatic deletion
RETENTION_HOURS=1

# How often the cleanup job runs, in minutes
CLEANUP_INTERVAL_MINUTES=15

# Application logging level: DEBUG, INFO, WARNING, ERROR
LOG_LEVEL=INFO

# Optional maximum upload size in megabytes. Default is 500 MB.
# MAX_FILE_SIZE_MB=500

# Comma-separated frontend origins for local development
# CORS_ORIGINS=http://localhost:3000,http://127.0.0.1:3000

# Path to a Netscape-format cookies.txt exported from a logged-in YouTube
# session. Used to get past YouTube's "Sign in to confirm you're not a bot"
# anti-bot check. Optional - defaults to backend/cookies/youtube_cookies.txt;
# if that file doesn't exist, requests go out without cookies as before.
# YOUTUBE_COOKIES_FILE=/app/backend/cookies/youtube_cookies.txt

# Alternative to YOUTUBE_COOKIES_FILE: read cookies live from a local
# browser instead of a static export. Set this to the browser name
# (firefox, chrome, chromium, edge, brave, vivaldi, opera, safari, whale)
# AND bind-mount that browser's profile directory - see the root
# vocal-separator/.env.example for YOUTUBE_BROWSER_PROFILE_HOST_PATH.
# Reliable only for Firefox-based browsers; see config.py for why.
# YOUTUBE_COOKIES_BROWSER=firefox

# Linux-only: keyring backend for decrypting Chromium-based browser cookies
# (BASICTEXT, GNOMEKEYRING, KWALLET, KWALLET5, KWALLET6). Not needed for
# Firefox.
# YOUTUBE_COOKIES_KEYRING=


# ========== OPTIMIZATION SETTINGS ==========

# Audio output format: mp3 (default, 80% smaller) or wav (uncompressed, higher quality)
AUDIO_OUTPUT_FORMAT=mp3

# MP3 bitrate: 128k (default, optimized), 192k, 256k, 320k
# Higher bitrate = better quality but larger file size. 128k is transparent for voice.
MP3_BITRATE=128k

# Model idle timeout in seconds: unload from RAM if idle to save ~80 MB.
# Default 3600 (1h) keeps the model warm between songs so there is no
# reload delay at the start of each task. Set to 0 to never unload.
MODEL_IDLE_TIMEOUT_SECONDS=3600
