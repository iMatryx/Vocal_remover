# API web and CORS middleware
fastapi==0.104.1

# ASGI server for running the FastAPI application
uvicorn[standard]==0.24.0

# Distributed task queue for background audio separation jobs
celery==5.3.4

# Redis client used as the Celery broker and result backend
redis==5.0.1

# Hybrid Transformer Demucs: state-of-the-art vocal/instrumental separation
demucs==4.0.1

# PyTorch backend for Demucs (CPU wheels installed from the PyTorch index)
torch==2.5.1
torchaudio==2.5.1

# Pinned below 2.0 to stay ABI-compatible with the torch wheels
numpy==1.26.4

# Backend torchaudio uses to write the separated WAV stems
soundfile==0.12.1

# Multipart form parsing for uploaded audio files
python-multipart==0.0.6

# Request and response data validation for FastAPI
pydantic==2.4.2

# Loading configuration values from environment files
python-dotenv==1.0.0

# Audio format conversion for MP3 compression (optimization)
pydub==0.25.1

# Audio analysis and processing (for audio feature extraction and conversion)
librosa==0.10.0

# Scientific computing (required by librosa for audio processing)
scipy==1.11.4

# FFmpeg wrapper for advanced audio operations (optional but recommended)
ffmpeg-python==0.2.0

# YouTube audio extraction (Feature 7) - updated frequently to keep up with
# YouTube's internal changes; bump this if extraction starts failing.
yt-dlp==2026.8.19

# yt-dlp plugin (auto-registers via the yt_dlp_plugins namespace, no import
# needed in our code) that fetches PO Tokens from the companion
# "bgutil-provider" service in docker-compose.yml, to get past YouTube's
# newer anti-bot checks without cookies or an account. Check
# https://github.com/Brainicism/bgutil-ytdlp-pot-provider for newer
# versions - this is a fast-moving cat-and-mouse dependency.
bgutil-ytdlp-pot-provider==2.0.0

# Memory profiling for optimization monitoring (Phase 2)
psutil==6.0.0
